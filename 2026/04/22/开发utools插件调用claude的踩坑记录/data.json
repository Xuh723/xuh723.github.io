{"title":"开发 uTools 插件调用 Claude Code CLI 的踩坑记录","date":"2026-04-21T16:15:00.000Z","toc":true,"source":"_posts/2026-04-22-开发utools插件调用claude的踩坑记录.md","raw":"---\ntitle: 开发 uTools 插件调用 Claude Code CLI 的踩坑记录\ndate: 2026-04-22 00:15:00\ntags:\n  - 工程备忘\ntoc: true\n---\n\n## 概述\n\n最近想在 uTools 里直接问 Claude 问题，不需要打开终端、不需要切窗口，输入完直接出结果。思路很简单：uTools 超级面板输入问题，调用本地 Claude Code CLI，拿到回答后展示在插件窗口里。\n\n听起来不复杂，实际写的时候踩了不少坑。主要是 uTools 基于 Electron，插件分两层上下文，加上 Claude Code 本身是个 TUI 工具，非交互调用很容易出问题。这篇文章把整个过程和踩的坑记下来，方便以后参考。\n\n## 插件目录结构\n\nuTools 插件本质上是一个文件夹，里面包含：\n\n```\nutools-claude-ask/\n├── plugin.json      # 插件配置\n├── preload.js       # Node 层\n├── index.html       # UI 层\n└── logo.png         # 图标\n```\n\n`plugin.json` 定义插件名称、入口文件和 feature 列表。`preload.js` 跑在 Node.js 上下文，可以调 `child_process` 这些原生模块。`index.html` 跑在沙盒浏览器里，两者通过 Electron 的 `contextBridge`（或直接挂载 window）通信。\n\n## uTools 插件的两层上下文\n\n这是第一个要理解清楚的地方。uTools 基于 Electron，插件代码分两个环境跑：\n\n| 层级 | 文件 | 权限 |\n|------|------|------|\n| Preload | `preload.js` | 可以 `require('child_process')`，有完整系统权限 |\n| Renderer | `index.html` | 只能访问 Web API + Preload 暴露出来的接口 |\n\n`preload.js` 通过 `window.exports` 注册 feature 的生命周期回调（`enter` / `search` / `select`），同时在 `window` 上挂载供前端调用的 API。前端调用后，实际执行系统命令的逻辑都在 `preload.js` 里完成。\n\n## 踩坑一：API 暴露失败\n\n一开始在 `preload.js` 里直接写：\n\n```javascript\nwindow.askClaude = (question) => { ... };\n```\n\n然后在 `index.html` 里发现 `window.askClaude` 是 `undefined`。\n\n原因：Electron 默认开了 `contextIsolation`，`preload.js` 的 `window` 和 `index.html` 的 `window` 不是同一个对象，直接挂载不生效。\n\n解决：兼容新旧版本，优先用 `contextBridge`， fallback 到直接挂载：\n\n```javascript\nfunction exposeAPI(name, api) {\n  try {\n    const { contextBridge } = require('electron');\n    contextBridge.exposeInMainWorld(name, api);\n  } catch (e) {\n    window[name] = api;\n  }\n}\n\nexposeAPI('askClaude', askClaude);\n```\n\n## 踩坑二：utools.redirect 跳转失败\n\n`select` 回调里调了 `utools.redirect('claude-ask-window')`，结果窗口没弹出来。\n\n原因：`plugin.json` 的 `features` 数组里没声明 `claude-ask-window` 这个 code。`redirect` 只能跳到已注册的 feature。\n\n解决：在 `plugin.json` 里补上：\n\n```json\n{\n  \"code\": \"claude-ask-window\",\n  \"explain\": \"Claude 回答窗口\",\n  \"cmds\": [],\n  \"mode\": \"window\"\n}\n```\n\n`cmds` 留空就不会被关键字触发，但可以通过 `redirect` 正常打开。\n\n## 踩坑三：Claude Code CLI 的非交互调用\n\n最初以为像普通 CLI 一样传个 `--stdin` 就行：\n\n```javascript\nspawn('claude', ['--stdin'], { ... });\n```\n\n结果进程直接挂了。Claude Code 本质上是 TUI 工具（基于 blessed 这类库），在非 TTY 子进程里行为很不稳定。而且 `--stdin` 这个参数根本不存在。\n\n解决思路：\n- 去掉不确定的参数，直接 `spawn('claude', [], ...)`\n- 通过 `child.stdin.write(question + '\\n')` 把问题写进去\n- 延迟几秒再写 `/exit\\n` 结束会话\n- 加上环境变量 `CI: '1'` 尝试让它别进 TUI 模式\n\n```javascript\nconst child = spawn('claude', [], {\n  cwd: getCwd(),\n  env: { ...process.env, FORCE_COLOR: '0', NO_COLOR: '1', CI: '1' },\n  shell: true\n});\n\nchild.stdin.write(question + '\\n');\nsetTimeout(() => { child.stdin.write('/exit\\n'); child.stdin.end(); }, 15000);\n```\n\n另外加了个预检，插件启动时执行 `claude --version`，把结果通过 `claudeStatus` 暴露给前端，方便用户第一时间知道 CLI 是否装好了。\n\n## 踩坑四：子进程找不到 claude 命令\n\n终端里 `claude` 能跑，插件里 `spawn` 就报 `ENOENT`。\n\n原因：uTools 启动的子进程继承的是它自己的环境变量，和用户 zsh/bash 配置文件里改过的 `PATH` 可能不一样。尤其是 npm 全局安装的工具，路径经常不在系统默认 PATH 里。\n\n解决：`spawn` 时传 `shell: true`，让 shell 去解析命令。同时前端顶部加状态栏，绿色表示检测通过，红色显示具体错误信息。\n\n## 踩坑五：工作目录硬编码\n\n一开始把默认 `cwd` 写死成了本地 vault 路径，结果代码里带了个人目录信息，换台机器直接用不了。\n\n优化后：\n- `getCwd()` 未配置时返回 `null`\n- 启动前检查，缺失直接报错：`未设置工作目录。请在插件界面输入工作目录路径并保存。`\n- UI 上加了个输入框 + 保存按钮，持久化到 `utools.db`\n\n```javascript\nfunction getCwd() {\n  try {\n    const doc = utools.db.get('claude-cwd');\n    if (doc && doc.data) return doc.data;\n  } catch (e) {}\n  return null;\n}\n```\n\n## utools.db 数据持久化\n\nuTools 内置了简单的键值存储，底层是 PouchDB，支持乐观锁 `_rev`：\n\n```javascript\n// 写入\ntry {\n  const existing = utools.db.get('claude-cwd');\n  utools.db.put({\n    _id: 'claude-cwd',\n    data: path,\n    _rev: existing ? existing._rev : undefined\n  });\n} catch (e) {\n  utools.db.put({ _id: 'claude-cwd', data: path });\n}\n\n// 读取\nconst doc = utools.db.get('claude-cwd');\nconst value = doc && doc.data ? doc.data : null;\n```\n\n这个插件里存了两样东西：\n- `claude-last-question`：超级面板输入的问题内容\n- `claude-cwd`：用户配置的工作目录路径\n\n## UI 交互细节\n\n几个小点：\n\n**Enter 发送**：输入框监听 `keydown`，`e.key === 'Enter'` 时触发发送。\n\n**再问一个**：结果区域加了个\"再问一个\"按钮，点击后重置状态，显示输入框并自动聚焦，不需要重启插件。\n\n**复制结果**：优先用 `utools.copyText(text)`， fallback 到 `navigator.clipboard`。\n\n**关闭窗口**：调用 `utools.outPlugin()`。\n\n## 安全注意\n\n- 不硬编码个人目录路径。\n- 敏感路径必须报错提示用户手动配置，不要静默使用默认值。\n- `shell: true` 配合用户输入要小心命令注入。这个插件里没有拼接外部输入到命令里，风险可控。\n\n## 后续可优化方向\n\n- 流式输出：逐字显示回答，而不是等进程结束一次性展示。需要把 `child.stdout.on('data')` 实时推送到 renderer。\n- 伪终端支持：用 `node-pty` 启动 Claude Code，彻底解决 TUI 依赖问题（但会引入原生依赖，打包麻烦）。\n- 历史记录：把问答记录存到 db 里，支持翻查过往对话。\n\n## 参考资料\n\n- [uTools 开发者文档](https://www.u-tools.cn/docs/developer/information/file-structure.html)\n- [Claude Code CLI 文档](https://docs.anthropic.com/en/docs/claude-code/overview)\n- [Electron contextBridge API](https://www.electronjs.org/docs/latest/api/context-bridge)\n","slug":"开发utools插件调用claude的踩坑记录","published":true,"updated":"2026-04-22T03:37:39.966Z","_id":"cmo9igtus0000e3k9e5u2b3zk","comments":true,"layout":"post","photos":[],"html":"<h2 id=\"概述\"><a href=\"#概述\" class=\"headerlink\" title=\"概述\"></a>概述</h2><p>最近想在 uTools 里直接问 Claude 问题，不需要打开终端、不需要切窗口，输入完直接出结果。思路很简单：uTools 超级面板输入问题，调用本地 Claude Code CLI，拿到回答后展示在插件窗口里。</p>\n<p>听起来不复杂，实际写的时候踩了不少坑。主要是 uTools 基于 Electron，插件分两层上下文，加上 Claude Code 本身是个 TUI 工具，非交互调用很容易出问题。这篇文章把整个过程和踩的坑记下来，方便以后参考。</p>\n<h2 id=\"插件目录结构\"><a href=\"#插件目录结构\" class=\"headerlink\" title=\"插件目录结构\"></a>插件目录结构</h2><p>uTools 插件本质上是一个文件夹，里面包含：</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">utools-claude-ask/</span><br><span class=\"line\">├── plugin.json      # 插件配置</span><br><span class=\"line\">├── preload.js       # Node 层</span><br><span class=\"line\">├── index.html       # UI 层</span><br><span class=\"line\">└── logo.png         # 图标</span><br></pre></td></tr></table></figure>\n\n<p><code>plugin.json</code> 定义插件名称、入口文件和 feature 列表。<code>preload.js</code> 跑在 Node.js 上下文，可以调 <code>child_process</code> 这些原生模块。<code>index.html</code> 跑在沙盒浏览器里，两者通过 Electron 的 <code>contextBridge</code>（或直接挂载 window）通信。</p>\n<h2 id=\"uTools-插件的两层上下文\"><a href=\"#uTools-插件的两层上下文\" class=\"headerlink\" title=\"uTools 插件的两层上下文\"></a>uTools 插件的两层上下文</h2><p>这是第一个要理解清楚的地方。uTools 基于 Electron，插件代码分两个环境跑：</p>\n<table>\n<thead>\n<tr>\n<th>层级</th>\n<th>文件</th>\n<th>权限</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Preload</td>\n<td><code>preload.js</code></td>\n<td>可以 <code>require(&#39;child_process&#39;)</code>，有完整系统权限</td>\n</tr>\n<tr>\n<td>Renderer</td>\n<td><code>index.html</code></td>\n<td>只能访问 Web API + Preload 暴露出来的接口</td>\n</tr>\n</tbody></table>\n<p><code>preload.js</code> 通过 <code>window.exports</code> 注册 feature 的生命周期回调（<code>enter</code> &#x2F; <code>search</code> &#x2F; <code>select</code>），同时在 <code>window</code> 上挂载供前端调用的 API。前端调用后，实际执行系统命令的逻辑都在 <code>preload.js</code> 里完成。</p>\n<h2 id=\"踩坑一：API-暴露失败\"><a href=\"#踩坑一：API-暴露失败\" class=\"headerlink\" title=\"踩坑一：API 暴露失败\"></a>踩坑一：API 暴露失败</h2><p>一开始在 <code>preload.js</code> 里直接写：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"variable language_\">window</span>.<span class=\"property\">askClaude</span> = <span class=\"function\">(<span class=\"params\">question</span>) =&gt;</span> &#123; ... &#125;;</span><br></pre></td></tr></table></figure>\n\n<p>然后在 <code>index.html</code> 里发现 <code>window.askClaude</code> 是 <code>undefined</code>。</p>\n<p>原因：Electron 默认开了 <code>contextIsolation</code>，<code>preload.js</code> 的 <code>window</code> 和 <code>index.html</code> 的 <code>window</code> 不是同一个对象，直接挂载不生效。</p>\n<p>解决：兼容新旧版本，优先用 <code>contextBridge</code>， fallback 到直接挂载：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">function</span> <span class=\"title function_\">exposeAPI</span>(<span class=\"params\">name, api</span>) &#123;</span><br><span class=\"line\">  <span class=\"keyword\">try</span> &#123;</span><br><span class=\"line\">    <span class=\"keyword\">const</span> &#123; contextBridge &#125; = <span class=\"built_in\">require</span>(<span class=\"string\">&#x27;electron&#x27;</span>);</span><br><span class=\"line\">    contextBridge.<span class=\"title function_\">exposeInMainWorld</span>(name, api);</span><br><span class=\"line\">  &#125; <span class=\"keyword\">catch</span> (e) &#123;</span><br><span class=\"line\">    <span class=\"variable language_\">window</span>[name] = api;</span><br><span class=\"line\">  &#125;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"title function_\">exposeAPI</span>(<span class=\"string\">&#x27;askClaude&#x27;</span>, askClaude);</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"踩坑二：utools-redirect-跳转失败\"><a href=\"#踩坑二：utools-redirect-跳转失败\" class=\"headerlink\" title=\"踩坑二：utools.redirect 跳转失败\"></a>踩坑二：utools.redirect 跳转失败</h2><p><code>select</code> 回调里调了 <code>utools.redirect(&#39;claude-ask-window&#39;)</code>，结果窗口没弹出来。</p>\n<p>原因：<code>plugin.json</code> 的 <code>features</code> 数组里没声明 <code>claude-ask-window</code> 这个 code。<code>redirect</code> 只能跳到已注册的 feature。</p>\n<p>解决：在 <code>plugin.json</code> 里补上：</p>\n<figure class=\"highlight json\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"punctuation\">&#123;</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;code&quot;</span><span class=\"punctuation\">:</span> <span class=\"string\">&quot;claude-ask-window&quot;</span><span class=\"punctuation\">,</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;explain&quot;</span><span class=\"punctuation\">:</span> <span class=\"string\">&quot;Claude 回答窗口&quot;</span><span class=\"punctuation\">,</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;cmds&quot;</span><span class=\"punctuation\">:</span> <span class=\"punctuation\">[</span><span class=\"punctuation\">]</span><span class=\"punctuation\">,</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;mode&quot;</span><span class=\"punctuation\">:</span> <span class=\"string\">&quot;window&quot;</span></span><br><span class=\"line\"><span class=\"punctuation\">&#125;</span></span><br></pre></td></tr></table></figure>\n\n<p><code>cmds</code> 留空就不会被关键字触发，但可以通过 <code>redirect</code> 正常打开。</p>\n<h2 id=\"踩坑三：Claude-Code-CLI-的非交互调用\"><a href=\"#踩坑三：Claude-Code-CLI-的非交互调用\" class=\"headerlink\" title=\"踩坑三：Claude Code CLI 的非交互调用\"></a>踩坑三：Claude Code CLI 的非交互调用</h2><p>最初以为像普通 CLI 一样传个 <code>--stdin</code> 就行：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"title function_\">spawn</span>(<span class=\"string\">&#x27;claude&#x27;</span>, [<span class=\"string\">&#x27;--stdin&#x27;</span>], &#123; ... &#125;);</span><br></pre></td></tr></table></figure>\n\n<p>结果进程直接挂了。Claude Code 本质上是 TUI 工具（基于 blessed 这类库），在非 TTY 子进程里行为很不稳定。而且 <code>--stdin</code> 这个参数根本不存在。</p>\n<p>解决思路：</p>\n<ul>\n<li>去掉不确定的参数，直接 <code>spawn(&#39;claude&#39;, [], ...)</code></li>\n<li>通过 <code>child.stdin.write(question + &#39;\\n&#39;)</code> 把问题写进去</li>\n<li>延迟几秒再写 <code>/exit\\n</code> 结束会话</li>\n<li>加上环境变量 <code>CI: &#39;1&#39;</code> 尝试让它别进 TUI 模式</li>\n</ul>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">const</span> child = <span class=\"title function_\">spawn</span>(<span class=\"string\">&#x27;claude&#x27;</span>, [], &#123;</span><br><span class=\"line\">  <span class=\"attr\">cwd</span>: <span class=\"title function_\">getCwd</span>(),</span><br><span class=\"line\">  <span class=\"attr\">env</span>: &#123; ...process.<span class=\"property\">env</span>, <span class=\"attr\">FORCE_COLOR</span>: <span class=\"string\">&#x27;0&#x27;</span>, <span class=\"attr\">NO_COLOR</span>: <span class=\"string\">&#x27;1&#x27;</span>, <span class=\"attr\">CI</span>: <span class=\"string\">&#x27;1&#x27;</span> &#125;,</span><br><span class=\"line\">  <span class=\"attr\">shell</span>: <span class=\"literal\">true</span></span><br><span class=\"line\">&#125;);</span><br><span class=\"line\"></span><br><span class=\"line\">child.<span class=\"property\">stdin</span>.<span class=\"title function_\">write</span>(question + <span class=\"string\">&#x27;\\n&#x27;</span>);</span><br><span class=\"line\"><span class=\"built_in\">setTimeout</span>(<span class=\"function\">() =&gt;</span> &#123; child.<span class=\"property\">stdin</span>.<span class=\"title function_\">write</span>(<span class=\"string\">&#x27;/exit\\n&#x27;</span>); child.<span class=\"property\">stdin</span>.<span class=\"title function_\">end</span>(); &#125;, <span class=\"number\">15000</span>);</span><br></pre></td></tr></table></figure>\n\n<p>另外加了个预检，插件启动时执行 <code>claude --version</code>，把结果通过 <code>claudeStatus</code> 暴露给前端，方便用户第一时间知道 CLI 是否装好了。</p>\n<h2 id=\"踩坑四：子进程找不到-claude-命令\"><a href=\"#踩坑四：子进程找不到-claude-命令\" class=\"headerlink\" title=\"踩坑四：子进程找不到 claude 命令\"></a>踩坑四：子进程找不到 claude 命令</h2><p>终端里 <code>claude</code> 能跑，插件里 <code>spawn</code> 就报 <code>ENOENT</code>。</p>\n<p>原因：uTools 启动的子进程继承的是它自己的环境变量，和用户 zsh&#x2F;bash 配置文件里改过的 <code>PATH</code> 可能不一样。尤其是 npm 全局安装的工具，路径经常不在系统默认 PATH 里。</p>\n<p>解决：<code>spawn</code> 时传 <code>shell: true</code>，让 shell 去解析命令。同时前端顶部加状态栏，绿色表示检测通过，红色显示具体错误信息。</p>\n<h2 id=\"踩坑五：工作目录硬编码\"><a href=\"#踩坑五：工作目录硬编码\" class=\"headerlink\" title=\"踩坑五：工作目录硬编码\"></a>踩坑五：工作目录硬编码</h2><p>一开始把默认 <code>cwd</code> 写死成了本地 vault 路径，结果代码里带了个人目录信息，换台机器直接用不了。</p>\n<p>优化后：</p>\n<ul>\n<li><code>getCwd()</code> 未配置时返回 <code>null</code></li>\n<li>启动前检查，缺失直接报错：<code>未设置工作目录。请在插件界面输入工作目录路径并保存。</code></li>\n<li>UI 上加了个输入框 + 保存按钮，持久化到 <code>utools.db</code></li>\n</ul>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">function</span> <span class=\"title function_\">getCwd</span>(<span class=\"params\"></span>) &#123;</span><br><span class=\"line\">  <span class=\"keyword\">try</span> &#123;</span><br><span class=\"line\">    <span class=\"keyword\">const</span> doc = utools.<span class=\"property\">db</span>.<span class=\"title function_\">get</span>(<span class=\"string\">&#x27;claude-cwd&#x27;</span>);</span><br><span class=\"line\">    <span class=\"keyword\">if</span> (doc &amp;&amp; doc.<span class=\"property\">data</span>) <span class=\"keyword\">return</span> doc.<span class=\"property\">data</span>;</span><br><span class=\"line\">  &#125; <span class=\"keyword\">catch</span> (e) &#123;&#125;</span><br><span class=\"line\">  <span class=\"keyword\">return</span> <span class=\"literal\">null</span>;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"utools-db-数据持久化\"><a href=\"#utools-db-数据持久化\" class=\"headerlink\" title=\"utools.db 数据持久化\"></a>utools.db 数据持久化</h2><p>uTools 内置了简单的键值存储，底层是 PouchDB，支持乐观锁 <code>_rev</code>：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 写入</span></span><br><span class=\"line\"><span class=\"keyword\">try</span> &#123;</span><br><span class=\"line\">  <span class=\"keyword\">const</span> existing = utools.<span class=\"property\">db</span>.<span class=\"title function_\">get</span>(<span class=\"string\">&#x27;claude-cwd&#x27;</span>);</span><br><span class=\"line\">  utools.<span class=\"property\">db</span>.<span class=\"title function_\">put</span>(&#123;</span><br><span class=\"line\">    <span class=\"attr\">_id</span>: <span class=\"string\">&#x27;claude-cwd&#x27;</span>,</span><br><span class=\"line\">    <span class=\"attr\">data</span>: path,</span><br><span class=\"line\">    <span class=\"attr\">_rev</span>: existing ? existing.<span class=\"property\">_rev</span> : <span class=\"literal\">undefined</span></span><br><span class=\"line\">  &#125;);</span><br><span class=\"line\">&#125; <span class=\"keyword\">catch</span> (e) &#123;</span><br><span class=\"line\">  utools.<span class=\"property\">db</span>.<span class=\"title function_\">put</span>(&#123; <span class=\"attr\">_id</span>: <span class=\"string\">&#x27;claude-cwd&#x27;</span>, <span class=\"attr\">data</span>: path &#125;);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 读取</span></span><br><span class=\"line\"><span class=\"keyword\">const</span> doc = utools.<span class=\"property\">db</span>.<span class=\"title function_\">get</span>(<span class=\"string\">&#x27;claude-cwd&#x27;</span>);</span><br><span class=\"line\"><span class=\"keyword\">const</span> value = doc &amp;&amp; doc.<span class=\"property\">data</span> ? doc.<span class=\"property\">data</span> : <span class=\"literal\">null</span>;</span><br></pre></td></tr></table></figure>\n\n<p>这个插件里存了两样东西：</p>\n<ul>\n<li><code>claude-last-question</code>：超级面板输入的问题内容</li>\n<li><code>claude-cwd</code>：用户配置的工作目录路径</li>\n</ul>\n<h2 id=\"UI-交互细节\"><a href=\"#UI-交互细节\" class=\"headerlink\" title=\"UI 交互细节\"></a>UI 交互细节</h2><p>几个小点：</p>\n<p><strong>Enter 发送</strong>：输入框监听 <code>keydown</code>，<code>e.key === &#39;Enter&#39;</code> 时触发发送。</p>\n<p><strong>再问一个</strong>：结果区域加了个”再问一个”按钮，点击后重置状态，显示输入框并自动聚焦，不需要重启插件。</p>\n<p><strong>复制结果</strong>：优先用 <code>utools.copyText(text)</code>， fallback 到 <code>navigator.clipboard</code>。</p>\n<p><strong>关闭窗口</strong>：调用 <code>utools.outPlugin()</code>。</p>\n<h2 id=\"安全注意\"><a href=\"#安全注意\" class=\"headerlink\" title=\"安全注意\"></a>安全注意</h2><ul>\n<li>不硬编码个人目录路径。</li>\n<li>敏感路径必须报错提示用户手动配置，不要静默使用默认值。</li>\n<li><code>shell: true</code> 配合用户输入要小心命令注入。这个插件里没有拼接外部输入到命令里，风险可控。</li>\n</ul>\n<h2 id=\"后续可优化方向\"><a href=\"#后续可优化方向\" class=\"headerlink\" title=\"后续可优化方向\"></a>后续可优化方向</h2><ul>\n<li>流式输出：逐字显示回答，而不是等进程结束一次性展示。需要把 <code>child.stdout.on(&#39;data&#39;)</code> 实时推送到 renderer。</li>\n<li>伪终端支持：用 <code>node-pty</code> 启动 Claude Code，彻底解决 TUI 依赖问题（但会引入原生依赖，打包麻烦）。</li>\n<li>历史记录：把问答记录存到 db 里，支持翻查过往对话。</li>\n</ul>\n<h2 id=\"参考资料\"><a href=\"#参考资料\" class=\"headerlink\" title=\"参考资料\"></a>参考资料</h2><ul>\n<li><a href=\"https://www.u-tools.cn/docs/developer/information/file-structure.html\">uTools 开发者文档</a></li>\n<li><a href=\"https://docs.anthropic.com/en/docs/claude-code/overview\">Claude Code CLI 文档</a></li>\n<li><a href=\"https://www.electronjs.org/docs/latest/api/context-bridge\">Electron contextBridge API</a></li>\n</ul>\n","excerpt":"","more":"<h2 id=\"概述\"><a href=\"#概述\" class=\"headerlink\" title=\"概述\"></a>概述</h2><p>最近想在 uTools 里直接问 Claude 问题，不需要打开终端、不需要切窗口，输入完直接出结果。思路很简单：uTools 超级面板输入问题，调用本地 Claude Code CLI，拿到回答后展示在插件窗口里。</p>\n<p>听起来不复杂，实际写的时候踩了不少坑。主要是 uTools 基于 Electron，插件分两层上下文，加上 Claude Code 本身是个 TUI 工具，非交互调用很容易出问题。这篇文章把整个过程和踩的坑记下来，方便以后参考。</p>\n<h2 id=\"插件目录结构\"><a href=\"#插件目录结构\" class=\"headerlink\" title=\"插件目录结构\"></a>插件目录结构</h2><p>uTools 插件本质上是一个文件夹，里面包含：</p>\n<figure class=\"highlight plaintext\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">utools-claude-ask/</span><br><span class=\"line\">├── plugin.json      # 插件配置</span><br><span class=\"line\">├── preload.js       # Node 层</span><br><span class=\"line\">├── index.html       # UI 层</span><br><span class=\"line\">└── logo.png         # 图标</span><br></pre></td></tr></table></figure>\n\n<p><code>plugin.json</code> 定义插件名称、入口文件和 feature 列表。<code>preload.js</code> 跑在 Node.js 上下文，可以调 <code>child_process</code> 这些原生模块。<code>index.html</code> 跑在沙盒浏览器里，两者通过 Electron 的 <code>contextBridge</code>（或直接挂载 window）通信。</p>\n<h2 id=\"uTools-插件的两层上下文\"><a href=\"#uTools-插件的两层上下文\" class=\"headerlink\" title=\"uTools 插件的两层上下文\"></a>uTools 插件的两层上下文</h2><p>这是第一个要理解清楚的地方。uTools 基于 Electron，插件代码分两个环境跑：</p>\n<table>\n<thead>\n<tr>\n<th>层级</th>\n<th>文件</th>\n<th>权限</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Preload</td>\n<td><code>preload.js</code></td>\n<td>可以 <code>require(&#39;child_process&#39;)</code>，有完整系统权限</td>\n</tr>\n<tr>\n<td>Renderer</td>\n<td><code>index.html</code></td>\n<td>只能访问 Web API + Preload 暴露出来的接口</td>\n</tr>\n</tbody></table>\n<p><code>preload.js</code> 通过 <code>window.exports</code> 注册 feature 的生命周期回调（<code>enter</code> &#x2F; <code>search</code> &#x2F; <code>select</code>），同时在 <code>window</code> 上挂载供前端调用的 API。前端调用后，实际执行系统命令的逻辑都在 <code>preload.js</code> 里完成。</p>\n<h2 id=\"踩坑一：API-暴露失败\"><a href=\"#踩坑一：API-暴露失败\" class=\"headerlink\" title=\"踩坑一：API 暴露失败\"></a>踩坑一：API 暴露失败</h2><p>一开始在 <code>preload.js</code> 里直接写：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"variable language_\">window</span>.<span class=\"property\">askClaude</span> = <span class=\"function\">(<span class=\"params\">question</span>) =&gt;</span> &#123; ... &#125;;</span><br></pre></td></tr></table></figure>\n\n<p>然后在 <code>index.html</code> 里发现 <code>window.askClaude</code> 是 <code>undefined</code>。</p>\n<p>原因：Electron 默认开了 <code>contextIsolation</code>，<code>preload.js</code> 的 <code>window</code> 和 <code>index.html</code> 的 <code>window</code> 不是同一个对象，直接挂载不生效。</p>\n<p>解决：兼容新旧版本，优先用 <code>contextBridge</code>， fallback 到直接挂载：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">function</span> <span class=\"title function_\">exposeAPI</span>(<span class=\"params\">name, api</span>) &#123;</span><br><span class=\"line\">  <span class=\"keyword\">try</span> &#123;</span><br><span class=\"line\">    <span class=\"keyword\">const</span> &#123; contextBridge &#125; = <span class=\"built_in\">require</span>(<span class=\"string\">&#x27;electron&#x27;</span>);</span><br><span class=\"line\">    contextBridge.<span class=\"title function_\">exposeInMainWorld</span>(name, api);</span><br><span class=\"line\">  &#125; <span class=\"keyword\">catch</span> (e) &#123;</span><br><span class=\"line\">    <span class=\"variable language_\">window</span>[name] = api;</span><br><span class=\"line\">  &#125;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"title function_\">exposeAPI</span>(<span class=\"string\">&#x27;askClaude&#x27;</span>, askClaude);</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"踩坑二：utools-redirect-跳转失败\"><a href=\"#踩坑二：utools-redirect-跳转失败\" class=\"headerlink\" title=\"踩坑二：utools.redirect 跳转失败\"></a>踩坑二：utools.redirect 跳转失败</h2><p><code>select</code> 回调里调了 <code>utools.redirect(&#39;claude-ask-window&#39;)</code>，结果窗口没弹出来。</p>\n<p>原因：<code>plugin.json</code> 的 <code>features</code> 数组里没声明 <code>claude-ask-window</code> 这个 code。<code>redirect</code> 只能跳到已注册的 feature。</p>\n<p>解决：在 <code>plugin.json</code> 里补上：</p>\n<figure class=\"highlight json\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"punctuation\">&#123;</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;code&quot;</span><span class=\"punctuation\">:</span> <span class=\"string\">&quot;claude-ask-window&quot;</span><span class=\"punctuation\">,</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;explain&quot;</span><span class=\"punctuation\">:</span> <span class=\"string\">&quot;Claude 回答窗口&quot;</span><span class=\"punctuation\">,</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;cmds&quot;</span><span class=\"punctuation\">:</span> <span class=\"punctuation\">[</span><span class=\"punctuation\">]</span><span class=\"punctuation\">,</span></span><br><span class=\"line\">  <span class=\"attr\">&quot;mode&quot;</span><span class=\"punctuation\">:</span> <span class=\"string\">&quot;window&quot;</span></span><br><span class=\"line\"><span class=\"punctuation\">&#125;</span></span><br></pre></td></tr></table></figure>\n\n<p><code>cmds</code> 留空就不会被关键字触发，但可以通过 <code>redirect</code> 正常打开。</p>\n<h2 id=\"踩坑三：Claude-Code-CLI-的非交互调用\"><a href=\"#踩坑三：Claude-Code-CLI-的非交互调用\" class=\"headerlink\" title=\"踩坑三：Claude Code CLI 的非交互调用\"></a>踩坑三：Claude Code CLI 的非交互调用</h2><p>最初以为像普通 CLI 一样传个 <code>--stdin</code> 就行：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"title function_\">spawn</span>(<span class=\"string\">&#x27;claude&#x27;</span>, [<span class=\"string\">&#x27;--stdin&#x27;</span>], &#123; ... &#125;);</span><br></pre></td></tr></table></figure>\n\n<p>结果进程直接挂了。Claude Code 本质上是 TUI 工具（基于 blessed 这类库），在非 TTY 子进程里行为很不稳定。而且 <code>--stdin</code> 这个参数根本不存在。</p>\n<p>解决思路：</p>\n<ul>\n<li>去掉不确定的参数，直接 <code>spawn(&#39;claude&#39;, [], ...)</code></li>\n<li>通过 <code>child.stdin.write(question + &#39;\\n&#39;)</code> 把问题写进去</li>\n<li>延迟几秒再写 <code>/exit\\n</code> 结束会话</li>\n<li>加上环境变量 <code>CI: &#39;1&#39;</code> 尝试让它别进 TUI 模式</li>\n</ul>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">const</span> child = <span class=\"title function_\">spawn</span>(<span class=\"string\">&#x27;claude&#x27;</span>, [], &#123;</span><br><span class=\"line\">  <span class=\"attr\">cwd</span>: <span class=\"title function_\">getCwd</span>(),</span><br><span class=\"line\">  <span class=\"attr\">env</span>: &#123; ...process.<span class=\"property\">env</span>, <span class=\"attr\">FORCE_COLOR</span>: <span class=\"string\">&#x27;0&#x27;</span>, <span class=\"attr\">NO_COLOR</span>: <span class=\"string\">&#x27;1&#x27;</span>, <span class=\"attr\">CI</span>: <span class=\"string\">&#x27;1&#x27;</span> &#125;,</span><br><span class=\"line\">  <span class=\"attr\">shell</span>: <span class=\"literal\">true</span></span><br><span class=\"line\">&#125;);</span><br><span class=\"line\"></span><br><span class=\"line\">child.<span class=\"property\">stdin</span>.<span class=\"title function_\">write</span>(question + <span class=\"string\">&#x27;\\n&#x27;</span>);</span><br><span class=\"line\"><span class=\"built_in\">setTimeout</span>(<span class=\"function\">() =&gt;</span> &#123; child.<span class=\"property\">stdin</span>.<span class=\"title function_\">write</span>(<span class=\"string\">&#x27;/exit\\n&#x27;</span>); child.<span class=\"property\">stdin</span>.<span class=\"title function_\">end</span>(); &#125;, <span class=\"number\">15000</span>);</span><br></pre></td></tr></table></figure>\n\n<p>另外加了个预检，插件启动时执行 <code>claude --version</code>，把结果通过 <code>claudeStatus</code> 暴露给前端，方便用户第一时间知道 CLI 是否装好了。</p>\n<h2 id=\"踩坑四：子进程找不到-claude-命令\"><a href=\"#踩坑四：子进程找不到-claude-命令\" class=\"headerlink\" title=\"踩坑四：子进程找不到 claude 命令\"></a>踩坑四：子进程找不到 claude 命令</h2><p>终端里 <code>claude</code> 能跑，插件里 <code>spawn</code> 就报 <code>ENOENT</code>。</p>\n<p>原因：uTools 启动的子进程继承的是它自己的环境变量，和用户 zsh&#x2F;bash 配置文件里改过的 <code>PATH</code> 可能不一样。尤其是 npm 全局安装的工具，路径经常不在系统默认 PATH 里。</p>\n<p>解决：<code>spawn</code> 时传 <code>shell: true</code>，让 shell 去解析命令。同时前端顶部加状态栏，绿色表示检测通过，红色显示具体错误信息。</p>\n<h2 id=\"踩坑五：工作目录硬编码\"><a href=\"#踩坑五：工作目录硬编码\" class=\"headerlink\" title=\"踩坑五：工作目录硬编码\"></a>踩坑五：工作目录硬编码</h2><p>一开始把默认 <code>cwd</code> 写死成了本地 vault 路径，结果代码里带了个人目录信息，换台机器直接用不了。</p>\n<p>优化后：</p>\n<ul>\n<li><code>getCwd()</code> 未配置时返回 <code>null</code></li>\n<li>启动前检查，缺失直接报错：<code>未设置工作目录。请在插件界面输入工作目录路径并保存。</code></li>\n<li>UI 上加了个输入框 + 保存按钮，持久化到 <code>utools.db</code></li>\n</ul>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">function</span> <span class=\"title function_\">getCwd</span>(<span class=\"params\"></span>) &#123;</span><br><span class=\"line\">  <span class=\"keyword\">try</span> &#123;</span><br><span class=\"line\">    <span class=\"keyword\">const</span> doc = utools.<span class=\"property\">db</span>.<span class=\"title function_\">get</span>(<span class=\"string\">&#x27;claude-cwd&#x27;</span>);</span><br><span class=\"line\">    <span class=\"keyword\">if</span> (doc &amp;&amp; doc.<span class=\"property\">data</span>) <span class=\"keyword\">return</span> doc.<span class=\"property\">data</span>;</span><br><span class=\"line\">  &#125; <span class=\"keyword\">catch</span> (e) &#123;&#125;</span><br><span class=\"line\">  <span class=\"keyword\">return</span> <span class=\"literal\">null</span>;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<h2 id=\"utools-db-数据持久化\"><a href=\"#utools-db-数据持久化\" class=\"headerlink\" title=\"utools.db 数据持久化\"></a>utools.db 数据持久化</h2><p>uTools 内置了简单的键值存储，底层是 PouchDB，支持乐观锁 <code>_rev</code>：</p>\n<figure class=\"highlight javascript\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 写入</span></span><br><span class=\"line\"><span class=\"keyword\">try</span> &#123;</span><br><span class=\"line\">  <span class=\"keyword\">const</span> existing = utools.<span class=\"property\">db</span>.<span class=\"title function_\">get</span>(<span class=\"string\">&#x27;claude-cwd&#x27;</span>);</span><br><span class=\"line\">  utools.<span class=\"property\">db</span>.<span class=\"title function_\">put</span>(&#123;</span><br><span class=\"line\">    <span class=\"attr\">_id</span>: <span class=\"string\">&#x27;claude-cwd&#x27;</span>,</span><br><span class=\"line\">    <span class=\"attr\">data</span>: path,</span><br><span class=\"line\">    <span class=\"attr\">_rev</span>: existing ? existing.<span class=\"property\">_rev</span> : <span class=\"literal\">undefined</span></span><br><span class=\"line\">  &#125;);</span><br><span class=\"line\">&#125; <span class=\"keyword\">catch</span> (e) &#123;</span><br><span class=\"line\">  utools.<span class=\"property\">db</span>.<span class=\"title function_\">put</span>(&#123; <span class=\"attr\">_id</span>: <span class=\"string\">&#x27;claude-cwd&#x27;</span>, <span class=\"attr\">data</span>: path &#125;);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 读取</span></span><br><span class=\"line\"><span class=\"keyword\">const</span> doc = utools.<span class=\"property\">db</span>.<span class=\"title function_\">get</span>(<span class=\"string\">&#x27;claude-cwd&#x27;</span>);</span><br><span class=\"line\"><span class=\"keyword\">const</span> value = doc &amp;&amp; doc.<span class=\"property\">data</span> ? doc.<span class=\"property\">data</span> : <span class=\"literal\">null</span>;</span><br></pre></td></tr></table></figure>\n\n<p>这个插件里存了两样东西：</p>\n<ul>\n<li><code>claude-last-question</code>：超级面板输入的问题内容</li>\n<li><code>claude-cwd</code>：用户配置的工作目录路径</li>\n</ul>\n<h2 id=\"UI-交互细节\"><a href=\"#UI-交互细节\" class=\"headerlink\" title=\"UI 交互细节\"></a>UI 交互细节</h2><p>几个小点：</p>\n<p><strong>Enter 发送</strong>：输入框监听 <code>keydown</code>，<code>e.key === &#39;Enter&#39;</code> 时触发发送。</p>\n<p><strong>再问一个</strong>：结果区域加了个”再问一个”按钮，点击后重置状态，显示输入框并自动聚焦，不需要重启插件。</p>\n<p><strong>复制结果</strong>：优先用 <code>utools.copyText(text)</code>， fallback 到 <code>navigator.clipboard</code>。</p>\n<p><strong>关闭窗口</strong>：调用 <code>utools.outPlugin()</code>。</p>\n<h2 id=\"安全注意\"><a href=\"#安全注意\" class=\"headerlink\" title=\"安全注意\"></a>安全注意</h2><ul>\n<li>不硬编码个人目录路径。</li>\n<li>敏感路径必须报错提示用户手动配置，不要静默使用默认值。</li>\n<li><code>shell: true</code> 配合用户输入要小心命令注入。这个插件里没有拼接外部输入到命令里，风险可控。</li>\n</ul>\n<h2 id=\"后续可优化方向\"><a href=\"#后续可优化方向\" class=\"headerlink\" title=\"后续可优化方向\"></a>后续可优化方向</h2><ul>\n<li>流式输出：逐字显示回答，而不是等进程结束一次性展示。需要把 <code>child.stdout.on(&#39;data&#39;)</code> 实时推送到 renderer。</li>\n<li>伪终端支持：用 <code>node-pty</code> 启动 Claude Code，彻底解决 TUI 依赖问题（但会引入原生依赖，打包麻烦）。</li>\n<li>历史记录：把问答记录存到 db 里，支持翻查过往对话。</li>\n</ul>\n<h2 id=\"参考资料\"><a href=\"#参考资料\" class=\"headerlink\" title=\"参考资料\"></a>参考资料</h2><ul>\n<li><a href=\"https://www.u-tools.cn/docs/developer/information/file-structure.html\">uTools 开发者文档</a></li>\n<li><a href=\"https://docs.anthropic.com/en/docs/claude-code/overview\">Claude Code CLI 文档</a></li>\n<li><a href=\"https://www.electronjs.org/docs/latest/api/context-bridge\">Electron contextBridge API</a></li>\n</ul>\n","path":"2026/04/22/开发utools插件调用claude的踩坑记录/","permalink":"https://xuh723.github.io/2026/04/22/%E5%BC%80%E5%8F%91utools%E6%8F%92%E4%BB%B6%E8%B0%83%E7%94%A8claude%E7%9A%84%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/","tags":[{"name":"工程备忘","_id":"cmo89dm6000038mk95ta7d3eg","slug":"工程备忘","path":"tags/工程备忘/","permalink":"https://xuh723.github.io/tags/%E5%B7%A5%E7%A8%8B%E5%A4%87%E5%BF%98/","length":5}],"categories":[],"prev":{"title":"搭建个人 AI 知识管理体系的实践记录","date":"2026-04-23T13:26:11.000Z","slug":"搭建个人AI知识管理体系的实践记录","published":true,"updated":"2026-04-23T13:32:02.783Z","_id":"cmobiurwm0000hhk91lri4eyx","layout":"post","photos":[],"excerpt":"","path":"2026/04/23/搭建个人AI知识管理体系的实践记录/","permalink":"https://xuh723.github.io/2026/04/23/%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BAAI%E7%9F%A5%E8%AF%86%E7%AE%A1%E7%90%86%E4%BD%93%E7%B3%BB%E7%9A%84%E5%AE%9E%E8%B7%B5%E8%AE%B0%E5%BD%95/","__post":true},"next":{"title":"配置 GitHub SSH 密钥","date":"2026-04-21T13:00:00.000Z","slug":"配置github-ssh密钥","published":true,"updated":"2026-04-21T06:34:13.727Z","_id":"cmo89dm6300108mk90j6u5txi","layout":"post","photos":[],"excerpt":"","path":"2026/04/21/配置github-ssh密钥/","permalink":"https://xuh723.github.io/2026/04/21/%E9%85%8D%E7%BD%AEgithub-ssh%E5%AF%86%E9%92%A5/","__post":true},"__post":true}